# v1.1 Bedrock Proposer Design

- Proposer generates candidate actions prior to simulation.
- Default is off; enable only via explicit configuration.
- Must remain deterministic for a given fixture and seed.
